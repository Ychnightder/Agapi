<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewAgapiVERSION311CLEAN"
        tools:targetApi="31">


        <activity
            android:name=".ui.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Accueil" />
        <activity android:name=".ui.Associations" />
        <activity android:name=".ui.Login_Inscription" />
        <activity android:name=".ui.Page_association" />
        <activity android:name=".ui.Page_Don" />
        <activity android:name=".ui.Profil"/>
        <activity android:name=".ui.Profil_Admin"/>
    </application>

</manifest>